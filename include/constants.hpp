#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

const static int horizontalBlockCount = 30;
const static int verticalBlockCount = 20;
const static int blockSize = 30;
const static int windowWidth = blockSize * horizontalBlockCount;
const static int windowHeight = blockSize * verticalBlockCount;
const static int FPS = 60;

#endif